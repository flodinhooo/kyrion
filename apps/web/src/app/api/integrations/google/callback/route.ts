import { CORE_SERVICE_URL, requireApiSession } from "@/lib/server-auth";
export async function GET(request: Request) { const incoming = new URL(request.url); const destination = new URL("/plugins/google", incoming.origin); const auth = await requireApiSession(); if (auth instanceof Response) { destination.searchParams.set("google", "session"); return Response.redirect(destination); } const code = incoming.searchParams.get("code"); const state = incoming.searchParams.get("state"); if (!code || !state || incoming.searchParams.has("error")) { destination.searchParams.set("google", "denied"); return Response.redirect(destination); } try { const r = await fetch(`${CORE_SERVICE_URL}/v1/integrations/google/authorization/complete`, { method: "POST", headers: { Authorization: `Bearer ${auth.token}`, "Content-Type": "application/json" }, body: JSON.stringify({ code, state }) }); destination.searchParams.set("google", r.ok ? "connected" : "failed"); } catch { destination.searchParams.set("google", "failed"); } return Response.redirect(destination); }
