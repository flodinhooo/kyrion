export type CoreProvider = { id: string; name: string; description: string; locality: "LOCAL" | "CLOUD"; authentication: string; availability: "LIVE" | "IN_DEVELOPMENT" | "PLANNED"; capabilities: Array<{ id: string; name: string; description: string }> };
export type CoreConnection = { providerId: string; connectionId: string; status: string; displayIdentity: string; enabledCapabilities: string[]; availableCapabilities: string[]; createdAt: string; updatedAt: string; health: string | null };
export function isCoreProviderList(value: unknown): value is CoreProvider[] { return Array.isArray(value) && value.every((item) => !!item && typeof item === "object" && typeof (item as CoreProvider).id === "string" && typeof (item as CoreProvider).name === "string" && ["LOCAL", "CLOUD"].includes((item as CoreProvider).locality)); }
export function isCoreConnectionList(value: unknown): value is CoreConnection[] { return Array.isArray(value) && value.every((item) => !!item && typeof item === "object" && typeof (item as CoreConnection).providerId === "string" && typeof (item as CoreConnection).connectionId === "string" && typeof (item as CoreConnection).displayIdentity === "string"); }
